import React from 'react';
export class lazy extends React.Component{
  render(){
    return (
      <div>
        jQuery(document).ready(function(){'{'}"use strict";var t=$(window),o=$(document),e=$("#loading"),i=$("#posts").is(".active");t.on("scroll",function(){'{'}if(o.height()-t.height()-80</div>
    );
  }
}